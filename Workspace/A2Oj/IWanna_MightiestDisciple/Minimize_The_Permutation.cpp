#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    int n,a[110],i;
    cin>>t;
    while(t--)
    {
        cin>>n;
        for(i=0; i<n; i++)
            cin>>a[i];
        int mini=a[0];
        for(i=0; i<n; i++)
        {
            if(a[i]<mini)
            {
                mini=a[i];
                x=i;
            }
        }

        if(n%2)
        {
            for(i=0;i<n;i++)
            {
                if()
            }
        }
    }
}
