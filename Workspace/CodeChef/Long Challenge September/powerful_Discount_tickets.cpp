#include<bits/stdc++.h>
using namespace std;
int main()
{
    int N,M;
    vector<int>v;
    cin>>N>>M;
    for(int i=0; i<N; i++)
        cin>>v[i];
    sort(v.begin(),v.end());
    v[N-1]=v[i]/(1<<M-1);
    v[N-2]=v[i]/(1<<M)
           int sum=0;
    for(int i=N-1; i>-0; i--)
    {

    }
}
