#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define sc(n) scanf("%lld",&n)
int main()
{
    int t,kase=1;
    while(t--)
    {
        ll n,id,s,tp,m;
        ll M[m+2];
        sc(n);
        while(n--)
        {
            sc(id);
            sc(s);
            sc(tp);
            sc(m);
            for(ll i=0; i<m; i++)
                scanf("%lld",M[i]);
                ll res[n+2];
                for(ll i=0;i<n;i++)

        }
    }
}
